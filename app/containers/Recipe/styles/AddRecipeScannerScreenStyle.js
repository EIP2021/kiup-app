import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  barcodeContainer: {
    top: '30%',
    left: '10%',
    position: 'absolute',
    width: '80%',
    height: '40%',
  },
  barcode: {
    height: '90%',
    borderWidth: 1,
    borderRadius: 10,
    borderColor: 'white',
  },
});

export default styles;
